<UserControl x:Class="HireMe.Wpf.Views.CustomerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
  <Border BorderBrush="Black" BorderThickness="2" Margin="2,5,2,5">
    <StackPanel>
      <TextBlock x:Name="Customer_Id" 
                 Margin="3"/>
      <TextBox x:Name="Customer_Name" 
               Margin="3"
               FontFamily="Century Gothic"/>
      <TextBox x:Name="Customer_EmailAddress" 
               Margin="3"
               FontStyle="Italic" 
               FontSize="10"/>
      <Border BorderBrush="Pink" 
              BorderThickness="1"
              Margin="3">
        <ListBox x:Name="Items" IsEnabled="False" />
        <!--ReviewItemViewModel-->
      </Border>
    </StackPanel>
  </Border>
</UserControl>
