Copy HireMe folder to C:\, so that the mdf file's location is "C:\HireMe\OdbcDataSource\HireMeDatabase.mdf".

Otherwise, (and perhaps despite that attempt, I haven't tried it yet) you will have to reconfigure the HireMeOdbc.dsn that the HireMe.WcfService requires.